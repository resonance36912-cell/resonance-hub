# Copy to ~/.codex/config.toml (user-global) or <repo>/.codex/config.toml (repo-local).
# No public Hub MCP endpoint is registered. Tool access is supplied through
# approved RONSAS connectors and control-plane integrations.

[project]
instructions_file = "AGENTS.md"

[shell]
package_manager = "bun"
